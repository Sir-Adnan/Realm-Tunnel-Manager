# 🦀 Realm Tunnel Manager (Rust Edition)

> **مدیریت حرفه‌ای و آسان Realm با تمرکز بر نهایت سرعت و پایداری.**

این اسکریپت یک ابزار مدیریت (Manager) برای هسته‌ی قدرتمند **Realm** است. برخلاف ابزارهایی مثل Gost یا Socat، ابزار Realm با زبان **Rust** نوشته شده و از تکنیک **Zero-Copy** استفاده می‌کند که باعث می‌شود ترافیک بدون پردازش اضافی در RAM، مستقیماً توسط کرنل منتقل شود.

مناسب برای سرورهایی که با **تعداد کاربر بالا (۱۰,۰۰۰+)** یا محدودیت منابع سخت‌افزاری روبرو هستند.

---

## 🚀 ویژگی‌های کلیدی
- **هسته Realm (Rust):** استفاده از آخرین نسخه Realm با معماری مدرن.
- **مصرف منابع ناچیز:** تقریباً ۱/۱۰ ابزارهای مشابه (مثل Gost) رم مصرف می‌کند.
- **پنل مدیریت گرافیکی (TUI):** افزودن و حذف سرورها بدون نیاز به تایپ دستورات پیچیده.
- **سرویس پایدار (Systemd):** اجرای خودکار در پس‌زمینه با قابلیت Restart خودکار.
- **واچ‌داگ هوشمند (Watchdog):** نظارت بر Load سرور و ریستارت خودکار در صورت هنگ کردن.
- **مدیریت لاگ‌ها:** جلوگیری از پر شدن دیسک با Logrotate و قابلیت "سکوت مطلق" (Silent Mode).
- **پشتیبانی از IPv4 و IPv6:** به صورت همزمان.

---

## 📦 نصب و اجرا

برای نصب کافیست دستور زیر را در ترمینال سرور خود (اوبونتو، دبیان، سنت‌او‌اس و...) اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Sir-Adnan/realm-tunnel/main/realm.sh)

```

پس از نصب، می‌توانید در هر جای ترمینال با تایپ دستور زیر وارد منو شوید:

```bash
irealm

```

---

## 🛠 راهنمای منو

پس از اجرای اسکریپت، گزینه‌های زیر در دسترس شماست:

| گزینه | عملکرد | توضیحات |
| --- | --- | --- |
| **[1] Add Relay** | افزودن فوروارد جدید | انتقال ترافیک از پورت ایران به پورت سرور خارج (تانل). |
| **[2] Delete Relay** | حذف فوروارد | حذف کانفیگ‌های موجود. |
| **[3] Show Config** | نمایش کانفیگ | نمایش فایل `config.toml` برای بررسی دستی. |
| **[4] Edit Config** | ویرایش دستی | باز کردن فایل کانفیگ با ویرایشگر Nano. |
| **[5] Logs** | مدیریت لاگ‌ها | مشاهده لاگ زنده، خالی کردن لاگ‌ها و تغییر حالت دیباگ. |
| **[6] Auto-Restart** | تنظیم Watchdog | تنظیم ریستارت خودکار بر اساس فشار CPU (Load Average). |
| **[7] Uninstall** | حذف کامل | حذف اسکریپت، سرویس‌ها و فایل‌های Realm. |

---

## ⚡️ چرا Realm؟ (مقایسه فنی)

| ویژگی | Gost (Go) | Iptables (Kernel) | **Realm (Rust)** |
| --- | --- | --- | --- |
| **محل اجرا** | User Space | Kernel Space | User Space (Zero-Copy) |
| **مصرف CPU** | متوسط/بالا | عالی (کم) | **بسیار عالی** |
| **مصرف RAM** | بالا (GC) | ثابت و کم | **بسیار کم و ثابت** |
| **پایداری** | متوسط | بسیار بالا | **بسیار بالا** |
| **سهولت** | آسان | سخت | **آسان** |

**نکته مهم:** Realm فقط یک "لوله انتقال" (Relay) است. برخلاف Gost، امکاناتی مثل رمزنگاری (TLS/WSS) یا ساکس پروکسی را ندارد. این ابزار صرفاً برای انتقال ترافیک (مثلاً از ایران به سرور خارج) با **حداکثر سرعت** طراحی شده است.

---

## ⚙️ بهینه‌سازی برای تعداد کاربر بالا (۳۰k+)

اگر قصد دارید برای تعداد کاربر بسیار بالا (بالای ۱۰ هزار نفر) استفاده کنید، پیشنهاد می‌شود تنظیمات کرنل لینوکس را نیز بهینه کنید. دستورات زیر را قبل از اجرای اسکریپت بزنید:

```bash
cat <<EOF >> /etc/sysctl.conf
fs.file-max = 1048576
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.core.somaxconn = 65535
net.ipv4.tcp_tw_reuse = 1
net.ipv4.ip_local_port_range = 1024 65535
EOF
sysctl -p

```

---

## ⚖️ سلب مسئولیت

این اسکریپت صرفاً جهت مقاصد آموزشی و مدیریت شبکه طراحی شده است. مسئولیت استفاده از آن بر عهده کاربر می‌باشد.

```
