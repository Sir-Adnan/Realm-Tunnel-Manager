# 🦀 Realm Tunnel Manager (نسخه Rust)

> **جایگزین فوق‌سریع، سبک و مدرن برای Gost و Iptables.**

این اسکریپت یک **پنل مدیریت (TUI)** ساده و قدرتمند برای **Realm** است.
برخلاف ابزارهای قدیمی (مثل Gost یا Socat) که داده‌ها را در RAM کپی می‌کنند، **Realm** با زبان **Rust** نوشته شده و از تکنولوژی **Zero-Copy** استفاده می‌کند. این یعنی سرور شما می‌تواند **۳۰,۰۰۰+ کاربر همزمان** را با کمترین فشار روی CPU و RAM مدیریت کند.

![Rust](https://img.shields.io/badge/Core-Rust-orange?style=flat-square)
![Performance](https://img.shields.io/badge/Performance-Zero--Copy-green?style=flat-square)
![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)

---

## 🚀 ویژگی‌های کلیدی

* **🦀 هسته قدرتمند Rust:** استفاده از آخرین نسخه باینری Realm (پایداری ۱۰۰٪).
* **📉 مصرف منابع ناچیز:** مصرف رم حدود **۱۰ مگابایت** (در مقایسه با ۵۰۰ مگابایت در ابزارهای دیگر).
* **⚡ تکنولوژی Zero-Copy:** انتقال مستقیم دیتا در سطح کرنل (بدون فشار به CPU).
* **🖥️ منوی مدیریت آسان:** افزودن/حذف تانل‌ها بدون نیاز به کدنویسی یا ویرایش فایل.
* **🛡️ واچ‌داگ هوشمند (Watchdog):** نظارت لحظه‌ای بر سرور و ریستارت خودکار سرویس در صورت هنگ کردن.
* **📊 مدیریت حرفه‌ای لاگ:** حالت "سکوت مطلق" (Silent) برای جلوگیری از پر شدن دیسک.
* **🌐 دوال استک:** پشتیبانی کامل و همزمان از **IPv4** و **IPv6**.

---

## 📦 نصب و اجرا

کافیست دستور زیر را در ترمینال سرور خود (اوبونتو، دبیان و...) کپی و اجرا کنید:

```bash
bash <(curl -Ls --ipv4 https://raw.githubusercontent.com/Sir-Adnan/Realm-Tunnel-Manager/main/realm.sh)

```

بعد از نصب، هر زمان خواستید وارد منو شوید، دستور زیر را تایپ کنید:

```bash
irealm

```

---

## 🛠 راهنمای گزینه‌های منو

| گزینه | عملکرد | توضیحات |
| --- | --- | --- |
| **[1] 🚀 Add Relay** | **افزودن تانل** | انتقال ترافیک از پورت ایران به آی‌پی و پورت خارج. |
| **[2] 🗑️ Delete Relay** | **حذف** | حذف کانفیگ‌های فعال. |
| **[3] 🌐 Show Config** | **نمایش** | دیدن فایل خام `config.toml`. |
| **[4] 🔧 Edit Config** | **ویرایش دستی** | باز کردن فایل کانفیگ با ویرایشگر Nano. |
| **[5] 📊 Logs** | **مدیریت لاگ** | مشاهده لاگ زنده، خالی کردن لاگ‌ها و تغییر حالت دیباگ. |
| **[6] 🔄 Auto-Restart** | **واچ‌داگ** | تنظیم ریستارت خودکار بر اساس فشار CPU (Load Average). |
| **[7] 🗑️ Uninstall** | **حذف کامل** | پاک کردن کامل Realm و تمام فایل‌های مربوطه. |

---

## ⚡️ چرا باید به Realm کوچ کنید؟ (مقایسه فنی)

| ویژگی | 🐢 Gost (Go) | 🐢 Socat (C) | 🦀 Realm (Rust) |
| --- | --- | --- | --- |
| **معماری** | User Space | User Space | **Zero-Copy (Splice)** |
| **مصرف CPU** | متوسط / بالا | بالا | **بسیار ناچیز** |
| **مصرف RAM** | بالا (نوسان دارد) | بالا (Forking) | **بسیار کم و ثابت** |
| **پایداری** | خوب | ضعیف | **عالی** |
| **لگ (Latency)** | دارای نوسان | ناپایدار | **صاف و خطی** |

> **نکته مهم:** ابزار Realm فقط یک **Relay لایه ۴** (TCP/UDP) است. این ابزار از رمزنگاری (TLS/WSS) پشتیبانی نمی‌کند و صرفاً برای **انتقال ترافیک** با نهایت سرعت طراحی شده است.

---

## ⚙️ بهینه‌سازی برای ترافیک سنگین (۳۰k+ کاربر)

اگر سرور شما ترافیک بسیار بالایی دارد، حتماً قبل از اجرای اسکریپت، دستورات زیر را برای **تیونینگ کرنل لینوکس** اجرا کنید:

```bash
cat <<EOF >> /etc/sysctl.conf
fs.file-max = 1048576
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.core.somaxconn = 65535
net.ipv4.tcp_tw_reuse = 1
net.ipv4.ip_local_port_range = 1024 65535
EOF
sysctl -p

```

---

## ⚖️ سلب مسئولیت

این اسکریپت صرفاً جهت مدیریت شبکه و اهداف آموزشی طراحی شده است. مسئولیت استفاده از آن بر عهده کاربر می‌باشد.
